EVALUATE
    TOPN(100, 'Sales')

EVALUATE
    CALCULATETABLE(
        'Sales',
        'Sales'[Quantity] > 10
    )

DEFINE 
MEASURE
    'Sales'[Total Sales] = SUM('Sales'[Sales Amount])
-- try to use the measure in a table
EVALUATE
    ADDCOLUMNS(
        'Sales',
        "Total Sales", [Total Sales]
    )

-- try to use the measure in a filter